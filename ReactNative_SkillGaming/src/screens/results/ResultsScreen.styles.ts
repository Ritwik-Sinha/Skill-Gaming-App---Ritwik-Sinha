import { StyleSheet } from 'react-native';
import { colors } from '../../theme';
import { resultGreen } from './resultPresentation';

export default StyleSheet.create({
  screen: { flex: 1 },
  content: {
    width: '100%',
    maxWidth: 680,
    alignSelf: 'center',
    paddingHorizontal: 22,
    paddingTop: 30,
    paddingBottom: 32,
  },
  eyebrow: {
    color: resultGreen,
    fontSize: 11,
    fontWeight: '800',
    letterSpacing: 2.5,
    marginBottom: 12,
  },
  heading: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 12,
  },
  title: {
    color: colors.text,
    fontSize: 34,
    lineHeight: 40,
    fontWeight: '800',
    letterSpacing: -1.2,
  },
  subtitle: {
    color: colors.muted,
    fontSize: 14,
    lineHeight: 21,
    marginTop: 10,
  },
  refresh: {
    width: 44,
    height: 44,
    borderRadius: 22,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#132B24',
    borderWidth: 1,
    borderColor: '#24503F',
  },
  refreshGlyph: { color: resultGreen, fontSize: 28, lineHeight: 34 },
  filters: { flexDirection: 'row', gap: 8, marginTop: 24, marginBottom: 20 },
  filter: {
    flex: 1,
    paddingVertical: 12,
    paddingHorizontal: 8,
    borderRadius: 20,
    backgroundColor: colors.surface,
    alignItems: 'center',
    borderWidth: 1,
    borderColor: colors.border,
  },
  selectedFilter: { backgroundColor: '#113B2E', borderColor: resultGreen },
  filterText: { color: colors.muted, fontSize: 12, fontWeight: '700' },
  selectedFilterText: { color: '#ACFFE6' },
  cards: { gap: 16 },
  card: {
    backgroundColor: '#151A18',
    borderWidth: 1,
    borderColor: '#2B3831',
    borderRadius: 24,
    padding: 18,
    overflow: 'hidden',
  },
  highlightedCard: { borderColor: resultGreen },
  cardHeader: { flexDirection: 'row', alignItems: 'center', gap: 12 },
  thumbnail: { width: 52, height: 52, borderRadius: 15, overflow: 'hidden' },
  gameCopy: { flex: 1 },
  gameName: { color: colors.text, fontSize: 17, fontWeight: '800' },
  date: { color: colors.inactive, fontSize: 11, marginTop: 5, lineHeight: 16 },
  entryBadge: { color: '#DCE8DF', fontSize: 12, fontWeight: '700' },
  status: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 7,
    marginTop: 21,
    flexWrap: 'wrap',
  },
  statusDot: { width: 6, height: 6, borderRadius: 3 },
  statusTitle: {
    fontSize: 17,
    lineHeight: 24,
    fontWeight: '800',
    flexShrink: 1,
  },
  matchLabel: { color: colors.muted, fontSize: 11, marginTop: 7 },
  description: {
    color: colors.muted,
    fontSize: 12,
    lineHeight: 19,
    marginTop: 8,
  },
  amounts: {
    borderTopWidth: 1,
    borderTopColor: '#2B3831',
    paddingTop: 13,
    gap: 9,
  },
  amountRow: { flexDirection: 'row', justifyContent: 'space-between', gap: 16 },
  amountLabel: { color: colors.muted, fontSize: 12, lineHeight: 18, flex: 1 },
  amountValue: {
    color: '#DDE8E0',
    fontSize: 12,
    lineHeight: 18,
    fontVariant: ['tabular-nums'],
    textAlign: 'right',
  },
  payoutLabel: { color: colors.text, fontSize: 14, fontWeight: '700' },
  payoutValue: {
    color: resultGreen,
    fontSize: 18,
    lineHeight: 23,
    fontWeight: '800',
    fontVariant: ['tabular-nums'],
  },
  feeNote: {
    color: colors.inactive,
    fontSize: 10,
    lineHeight: 16,
    marginTop: 3,
  },
  detailButton: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    borderRadius: 14,
    backgroundColor: '#21382C',
    minHeight: 45,
    marginTop: 16,
    paddingVertical: 11,
    paddingHorizontal: 16,
  },
  detailLabel: { color: '#BEF9DF', fontSize: 12, fontWeight: '800' },
  pressed: { opacity: 0.7 },
  disabled: { opacity: 0.5 },
  stateCard: {
    alignItems: 'center',
    backgroundColor: '#131C17',
    borderRadius: 24,
    borderWidth: 1,
    borderColor: '#2B3831',
    paddingHorizontal: 26,
    paddingVertical: 40,
    gap: 14,
  },
  stateTitle: {
    color: colors.text,
    textAlign: 'center',
    fontSize: 20,
    fontWeight: '800',
  },
  stateDescription: {
    color: colors.muted,
    fontSize: 13,
    lineHeight: 21,
    textAlign: 'center',
  },
  errorBanner: {
    backgroundColor: '#30231F',
    borderRadius: 16,
    padding: 16,
    marginBottom: 16,
    gap: 7,
  },
  errorText: { color: '#FFC1A9', fontSize: 12, lineHeight: 18 },
  retryButton: {
    minHeight: 44,
    justifyContent: 'center',
    paddingHorizontal: 14,
    borderRadius: 12,
    backgroundColor: '#413129',
    alignSelf: 'flex-start',
  },
  retryText: { color: '#FFD1BF', fontSize: 13, fontWeight: '800' },
  footer: {
    color: colors.inactive,
    fontSize: 11,
    lineHeight: 18,
    textAlign: 'center',
    marginTop: 22,
  },
});
